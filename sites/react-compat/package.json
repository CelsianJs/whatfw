{
  "name": "what-react-compat-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vite --port 4590",
    "build": "npx vite build",
    "preview": "npx vite preview --port 4590"
  }
}
